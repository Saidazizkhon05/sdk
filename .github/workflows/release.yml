name: Release SDK

on:
  push:
    branches:
      - main  # Adjust to your default branch if necessary

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: write  # Grants access to push tags

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Set up JDK
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: 17

      - name: Grant Execute Permission for Gradle
        run: chmod +x gradlew

      - name: Build SDK
        run: ./gradlew mylibrary:assembleRelease

      - name: Create Git Tag
        id: create_tag  # Assigning an ID to access the tag
        run: |
          TAG="v$(date +'%Y.%m.%d.%H%M')"
          echo "TAG=$TAG" >> $GITHUB_ENV
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag $TAG
          git push origin $TAG

      - name: Publish Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ env.TAG }}  # Referencing the created tag
          files: mylibrary/build/outputs/aar/mylibrary-release.aar
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
